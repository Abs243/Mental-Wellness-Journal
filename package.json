{
  "name": "mental-wellness-journal",
  "version": "1.0.0",
  "description": "Complete MERN Mental Wellness Journal Application",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run prod --prefix backend\" \"npm start --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix frontend -- --watchAll=false"
  },
  "devDependencies": {
    "chai": "^5.1.2",
    "concurrently": "^7.0.0",
    "mocha": "^11.0.1"
  },
  "author": "Mental Wellness Journal Team",
  "license": "ISC"
}
